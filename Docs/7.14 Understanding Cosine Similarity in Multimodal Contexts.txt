Welcome back, let's discuss cosine similarity. This is a fundamental tool in data science and machine learning that helps us measure how similar two objects are. So whether you're comparing text documents, images, even audio files, cosine similarity is powerful and intuitive for understanding relationships between different types of data. And by the end of this video, you'll understand what cosine similarity is and also how to apply it. Let's focus first on the core ideas. So let's start with this basic idea. Cosine similarity measures the cosine of the angle between two vectors in a multidimensional space. Imagine these vectors as arrows pointing from the origin of a graph to a different points. The closer the vectors are, then the smaller the angle between them and the more similar they are. Conversely, if the vectors point in very different directions, the angle is very large and the cosine similarity is low. Looking at it intuitively, if two objects are similar, they point in the same direction. Cosine similarity captures this by focusing on the direction rather than the magnitude of the vectors. Let's visualize this a bit better. So imagine that we have two arrows on a piece of paper. If these arrows are almost on top of each other, the angle between them is very small. This will result in the cosine similarity close to one, which means that they are very similar. Now, if the arrows point in opposite directions, the angle between them is large, making the cosine similarity close to minus one, indicating that they are dissimilar. If the arrows are perpendicular, so 90 degrees, cosine similarity will be zero, indicating no similarity at all. Therefore, as a key idea, the cosine similarity focuses on direction. When vectors representing, for instance, text documents or image features are aligned, their cosine similarity is high, meaning that they share similar characteristics. If we look at the mathematical formula, this is calculated as A times B. So A times B is a dot product of the vectors and of their corresponding elements, and then A and B. So these represent the magnitudes, so lengths of vectors. This is so that they're absolute. I think that's the correct word in English when we have two lines. And then the result is a value between minus one and one. Where one indicates the same, so identical direction, zero is orthogonality, no similarity, and minus one indicates opposite direction. If we look at a practical example with text documents, let's say that we have two of them. One is a job description for a data scientist, and the other one is a resume. We can represent each document as a vector where each dimension corresponds to a specific word and the value in that dimension is the frequency of the word in the document. As an example, so we have here document A, job description, data science, machine learning, Python statistics. Document B, which would be the resume, we would have Python statistics, machine learning, deep learning. We create vectors from these documents by counting the frequency of each word. Next, we compute the cosine similarity between these vectors. If it's high, indicates that the resume is a good match for the job description because the key terms align closely. Therefore, as a key idea, cosine similarity helps compare documents focusing on the presence and frequency of shared words. And this makes it easier to find the best match. If we apply it to images, so this isn't just for text. So imagine that we have two cats. You can represent each image as a vector of pixel values or as a set of features extracted by the model like clip. And we calculate the cosine similarity. So for instance, image A, a vector representing image of a cat sitting on a sofa, image B, a vector representing the image of a cat lying on a bed. And if these vectors have a high cosine similarity, it means that the images are visually similar, perhaps because the cats are in similar poses or the backgrounds are alike. Now let's cover real world applications. Here, we would have search engines. When you type a query into a search engine, cosine similarity helps match your query with the most relevant documents or webpages. Recommendation systems. So think Netflix, Amazon, they use it to recommend movies or products and image recognition. So in facial recognition systems, cosine similarity is used to match a person's face with images in the database. Now let's conclude here. So to wrap up, cosine similarity is very powerful for comparing things. So we cover here text with text, image with image, but as we'll see, we'll do text with image. And by measuring the angle between vectors, it gives us a sense of how similar they are, focusing on the direction rather than the magnitude. This makes it especially useful in applications like text analysis, image recognition, et cetera. And cosine similarity opens the door for more intuitive and effective ways of comparing data, as we'll be able to see. It's also very, very easy to apply, which is the best one. Now, as a last thing, I've covered this with simple examples and simple angles. But if you think about it, our embeddings are massive. Therefore, this is a multidimensional stuff. So it's very difficult. It's impossible for us. We can do three dimensions. This is multidimensional stuff. And therefore, visualizing how this works, it's a bit impossible for us. But the general idea is that we use cosine similarity to see how similar are our embeddings. Until the next video, have fun. 